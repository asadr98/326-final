from flashcards import Flash
from flashcards import Card
import pytest

def test_split_text_into_cards():
    "Tests split_text_into_cards method of Flash class."
    assert cards = [question, answer]

def test_Card():
    c = Card("alleviate", "to make better, easier")
    assert c.question == "alleviate"
    assert c.answer == "to make better, easier"